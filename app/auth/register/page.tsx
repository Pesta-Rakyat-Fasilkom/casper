import { Layout } from "@/components/elements/Layout";
import { Register } from "@/modules/auth/register";

const Page = () => {
  return (
    <Layout>
      <Register />
    </Layout>
  );
};

export default Page;
