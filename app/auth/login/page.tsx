import { Layout } from "@/components/elements/Layout";
import { Login } from "@/modules/auth/login";

const Page = () => {
  return (
    <Layout>
      <Login />
    </Layout>
  );
};

export default Page;
